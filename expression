#include<stdio.h>
#include<math.h>
#include<conio.h>
int main()
{
    int a,b,c;
    printf("Enter a a value: ");
    scanf("%d",&a);
    printf("Enter a b value: ");
    scanf("%d",&b);
    c=a*(a+b);
    printf("The result is %d", c);
    return 0;
}
